import { SearchDocumentPipe } from './search-document.pipe';

describe('SearchDocumentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDocumentPipe();
    expect(pipe).toBeTruthy();
  });
});
